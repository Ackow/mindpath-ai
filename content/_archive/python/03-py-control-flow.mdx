---
id: py-control-flow
title: 03.控制流与推导式
module: foundations
submodule: python
order: 3
difficulty: beginner
prerequisites: [py-basics]
relatedNotes: [py-functions, py-data-structures]
nextNotes: [py-data-structures]
map:
  layoutGroup: foundations-python
estimatedMinutes: 30
tags: [Python, 控制流, 条件分支, 循环, 推导式]
summary: 用条件、循环和推导式把输入数据转换为可解释、可测试的处理流程。
---

## 本节要解决什么问题？

模型训练脚本通常不是一条直线：需要先检查输入，再按条件选择策略，最后遍历批次并汇总结果。本节把这种“根据状态选择下一步”的思维翻译成 Python 语法。完成后，你应能写出清晰的分支、可终止的循环，并用推导式表达简单的数据转换。

<ConceptCard title="先画路径，再写语句" type="intuition">
把每个条件看成一个决策节点，把每次循环看成一次状态更新。先写清楚“何时进入、何时退出、每轮改变什么”，再选择 `if`、`for` 或 `while`，能显著减少死循环和遗漏分支。
</ConceptCard>

## 1. 条件分支：让程序做出选择

`if`、`elif`、`else` 使用布尔表达式决定代码块是否执行。缩进是语法的一部分，同一分支中的语句必须保持一致缩进。

```python
score = 72

if score >= 90:
    result = "优秀"
elif score >= 60:
    result = "及格"
else:
    result = "需要复习"

print(result)
```

条件应尽量表达一个业务判断；复杂判断可以先拆成有名字的布尔变量：

```python
has_data = len(samples) > 0
has_labels = len(labels) == len(samples)
ready = has_data and has_labels
```

### 1.1 条件的组合与嵌套

条件分支可以组合比较、逻辑运算和成员判断。先写出“最具体的情况”，再写范围更大的情况，避免前面的条件把后面的分支遮住：

```python
label = "cat"
confidence = 0.92

if label not in {"cat", "dog"}:
    decision = "未知类别"
elif confidence < 0.5:
    decision = "需要人工复核"
elif label == "cat":
    decision = "猫"
else:
    decision = "狗"

print(decision)
```

只有在分支内部还需要再次判断时才嵌套 `if`。如果嵌套超过两层，通常应该提取成函数或拆成具名布尔变量。简单的二选一可以使用条件表达式：

```python
score = 0.73
status = "通过" if score >= 0.6 else "未通过"
```

条件表达式适合一个短结果，不要把多条语句或复杂副作用塞进一行。

### 1.2 多个 `if` 与 `elif` 链

多个独立的 `if` 都会分别判断；`if/elif/else` 是一条互斥选择链，命中一个分支后就不再检查其余分支：

```python
score = 95

if score >= 60:              # 独立的 if：会执行
    print("及格")
if score >= 90:              # 另一个独立的 if：也会执行
    print("优秀")

if score >= 90:              # elif 链：只执行第一个满足的分支
    print("等级 A")
elif score >= 60:
    print("等级 B")
else:
    print("等级 C")
```

Python 用缩进表示代码块。推荐统一使用 4 个空格，`elif` 和 `else` 必须与 `if` 对齐；混用 Tab 和空格或少缩进一行，可能触发 `IndentationError`，也可能让语句落在错误分支中。

<FlowControlAnimation />

这个动画只演示本节最容易混淆的 `if / elif / else`，高亮流程图节点代表当前执行位置；可以手动输入“分数”和“及格阈值”，再点击播放或逐步按钮：分数为 95 会走 `if` 的“优秀”路径，72 会走 `elif` 的“及格”路径，低于阈值则走 `else`。

### 1.3 真值、短路与边界

空容器、`0`、`None` 会被当作假值；非空容器和非零数字通常是真值。`and` 与 `or` 会短路，因此可以安全地把前置检查放在前面：

```python
items = ["cat", "dog"]
if items and items[0] == "cat":		# items非空并且一项为"cat"
    print("第一项是 cat")
```

用 `is None` 判断缺失值，不要把 `is` 当作普通相等比较：`==` 比较内容，`is` 比较是否为同一个对象。

## 2. 循环：重复执行并维护状态

写循环前先回答三个问题：初始状态是什么？每轮循环改变什么？什么条件会停止？这三个答案分别对应初始化、循环体和退出条件。

### 2.1 `for`：遍历已知序列

`for` 适合遍历列表、字典、字符串或 `range`。循环体内只维护当前项和必要的累积状态：

```python
losses = [0.9, 0.6, 0.4]
total = 0.0
# 遍历losses列表每一项，累加到total
for loss in losses:
    total += loss
print(total / len(losses))
```

- **enumerate()**：

  - 作用：遍历同时拿到**下标+元素**

  - 语法：`enumerate(可迭代对象，start=0)`，start为下标从几开始，默认为0

  - ```python
    for idx, val in enumerate(nums, start=1):
        print(idx, val)
    """
    1 10
    2 20
    3 30
    """
    ```

- **zip()**：

  - 作用：**并行打包多个序列，按位置配对**，短的序列决定最终长度（最短对齐）

  - 语法：

  - ```python
    a = [1,2,3]
    b = ["x","y","z"]
    
    for va, vb in zip(a,b):
        print(va, vb)
    """
    1 x
    2 y
    3 z
    """
    ```

需要索引时优先使用 `enumerate`，同时遍历两个序列时使用 `zip`：

```python
for index, loss in enumerate(losses, start=1):
    print(f"step={index}, loss={loss:.2f}")
```

遍历字典时，直接遍历得到的是键；需要键和值时使用 `.items()`，只需要值时使用 `.values()`：

```python
metrics = {"accuracy": 0.91, "recall": 0.84}
for name, value in metrics.items():
    print(f"{name}: {value:.2f}")
```

`range(stop)` 生成从 `0` 到 `stop - 1` 的整数序列；`range(start, stop, step)` 可以指定起点和步长。`zip` 把多个序列按相同位置配对，并在最短的序列结束时停止，因此需要确认各序列长度是否应当一致。

```python
for epoch in range(1, 4):
    print("epoch", epoch)

names = ["train", "validation"]
counts = [120, 30]
for name, count in zip(names, counts):
    print(f"{name}: {count} samples")
```

`zip` 不会因为长度不同自动报错，而是静默按短序列截断。需要严格一一对应时，先比较长度：

```python
names = ["train", "validation"]
counts = [120]
print(list(zip(names, counts)))  # [('train', 120)]
if len(names) != len(counts):
    raise ValueError("names 和 counts 的长度必须一致")
```

<LoopFlowAnimation />

动画把 `for` 循环拆成“初始化、条件判断、循环体、更新状态、回到条件”的闭环。滚轮可缩放画布；播放或点击下一步时，当前连线会流动。`break` 边表示直接离开循环，条件为假则自然结束。

### 2.2 `while`：直到条件不再成立

`while` 适合重试、读取队列或迭代未知次数的过程。循环体必须改变退出条件，并设置上限或超时，避免无限循环：

```python
attempt = 0
while attempt < 3:
    attempt += 1
    print(f"第 {attempt} 次尝试")
```

处理“读到特殊值才结束”的任务时，可以使用哨兵值。下面的循环在读到 `None` 时停止，并且不会把哨兵当作数据处理：

```python
batch_stream = [[1, 2], [3, 4], None]
index = 0
while True:
    batch = batch_stream[index]
    index += 1
    if batch is None:
        break
    print("处理批次:", batch)
```

如果退出条件来自外部资源（文件、队列或网络），还要考虑异常和超时；在本地练习中至少设置最大尝试次数。

更常见的重试模式是“最多尝试几次，成功就提前停止”：

```python
max_attempts = 3
for attempt in range(1, max_attempts + 1):
    response_ok = attempt == 2  # 模拟第 2 次成功
    if response_ok:
        print(f"第 {attempt} 次成功")
        break
else:
    print("达到最大尝试次数，仍未成功")
```

### 2.3 `break`、`continue` 与 `else`

`continue` 跳过当前轮，`break` 立即离开循环。循环 `else` 只在循环自然结束、没有触发 `break` 时执行：

```python
for value in [2, 4, 7, 8]:
    if value % 2:
        print("找到奇数", value)
        break
else:
    print("全部是偶数")
```

例如筛选训练样本时，`continue` 可以跳过缺失值，避免把无效样本加入结果：

```python
scores = [0.8, None, 0.4, 0.9]
passed = []
for score in scores:
    if score is None:
        continue
    if score >= 0.6:
        passed.append(score)
print(passed)  # [0.8, 0.9]
```

`break` 只结束它所在的最内层循环。多层循环需要一起退出时，优先把内层逻辑提取成函数，或使用清楚命名的标志变量。

## 3. 推导式：把简单循环写成表达式

列表推导式的基本结构是 `[表达式 for 变量 in 序列 if 条件]`。先写出等价的普通循环，再压缩为推导式，便于检查逻辑：

```python
data = [1.2, -0.5, 3.4, -2.1, 0.8]
relu_output = [x if x > 0 else 0 for x in data]
print("ReLU 模拟滤波:", relu_output)
```

字典推导式适合构造键值映射，集合推导式会自动去重：

<RunnableCodeBlock title="表达式循环">
{`
probabilities = [0.12, 0.51, 0.93, 0.48]
labels = {i: ("positive" if p >= 0.5 else "negative") for i, p in enumerate(probabilities)}
unique_labels = {label for label in labels.values()}
print(labels, unique_labels)
`}
</RunnableCodeBlock>

推导式也可以带筛选条件，筛选条件写在 `for` 后面；如果同时需要转换和筛选，先读成普通语言再写代码：

```python
scores = [0.42, 0.81, 0.67, 0.39]
passed = [score for score in scores if score >= 0.6]
scaled = [round(score * 100) for score in passed]
print(passed, scaled)  # [0.81, 0.67] [81, 67]
```

不要混淆两种条件位置：`for` 后的 `if` 会**过滤**元素；表达式中的 `if ... else ...` 会**保留每个元素但转换它**：

```python
values = [-2, 0, 3]
positive_only = [x for x in values if x > 0]
relu_like = [x if x > 0 else 0 for x in values]
print(positive_only)  # [3]
print(relu_like)      # [0, 0, 3]
```

在 Python 3 中，推导式中的循环变量不会覆盖外层同名变量；普通 `for` 则会：

```python
item = "outside"
_ = [item * 2 for item in [1, 2]]
print(item)  # outside
```

多个 `for` 会形成嵌套循环。它适合很小的笛卡尔积，但数据量大时要警惕复杂度：

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
variants = [(color, size) for color in colors for size in sizes]
print(variants)
```

条件超过一层、表达式需要多行或包含副作用时，改回普通循环。可读性比少写几行代码更重要。

## 4. 循环复杂度：先减少不必要的工作

如果每个样本只处理一次，样本数翻倍，工作量大致也翻倍，记作 `O(n)`。两层循环都遍历同一批样本时，工作量大致变成四倍，记作 `O(n^2)`。这不是精确计时工具，而是提醒你在数据变大前检查循环结构。

```python
samples = ["a", "b", "c"]

# O(n): 每个样本处理一次
for sample in samples:
    print(sample)

# O(n^2): 每一对样本都要比较；这里只用于演示小数据
for left in samples:
    for right in samples:
        print(left, right)
```

后续使用 NumPy 和 pandas 时，优先寻找向量化、聚合或连接操作，而不是在大型数组和表格上手写嵌套 Python 循环。

## 小实验：批量筛选并汇总

运行下面的代码，修改 `threshold`，观察通过筛选的样本和均值如何变化：

<RunnableCodeBlock title="用条件和循环汇总样本">
{`scores = [0.42, 0.81, 0.67, 0.39, 0.93]
threshold = 0.6

passed = []
for score in scores:
    if score >= threshold:
        passed.append(score)

average = sum(passed) / len(passed) if passed else 0.0
print("通过数量:", len(passed))
print("通过均值:", round(average, 3))`}
</RunnableCodeBlock>

### 实验变式

1. 把 `threshold` 改成 `0.9`，观察没有样本通过时为什么仍然不会除以 0。
2. 把 `passed` 的普通 `for` 循环改写成列表推导式，并比较哪一种更容易调试。
3. 增加一个 `None` 分数，先用 `continue` 跳过它，再计算平均值。

### 综合练习：清洗一批分数

请独立完成这个流程：遍历 `raw_scores = [0.8, None, 1.2, 0.4, -0.1]`；遇到 `None` 时用 `continue` 跳过；遇到不在 `0` 到 `1` 范围内的值时打印异常值并用 `break` 停止；最后用推导式把已经处理的合法分数转换成百分制。完成后思考：提前 `break` 与“跳过所有异常值”的策略分别适用于什么任务？

## AI 场景：遍历一个小批次

训练循环通常会在每个 batch 上完成“读取、检查、计算、记录”四步。下面是一个不依赖第三方库的简化版本，重点是观察控制流，而不是训练模型：

<RunnableCodeBlock title="模拟训练轮次与批次日志">
{`batches = [[0.8, 0.7], [0.6, 0.5], [0.4, 0.3]]
stop_loss = 0.35

for epoch in range(1, 3):
    losses = []
    for batch_index, batch in enumerate(batches, start=1):
        batch_loss = sum(batch) / len(batch)
        losses.append(batch_loss)
        print(f"epoch={epoch}, batch={batch_index}, loss={batch_loss:.2f}")

    epoch_loss = sum(losses) / len(losses)
    print(f"epoch={epoch}, mean_loss={epoch_loss:.2f}")
    if epoch_loss <= stop_loss:
        print("达到目标，提前停止")
        break`}
</RunnableCodeBlock>

这里的外层 `for` 表示 epoch，内层 `for` 表示 batch，`break` 只会结束它所在的最内层循环。真实训练代码还会加入验证集、梯度更新和日志系统，但控制流骨架相同。

## 常见错误排查

| 现象 | 原因 | 处理方式 |
|---|---|---|
| 分支结果与预期不符 | 条件顺序错误，宽条件抢先命中 | 先写更具体的条件，再写兜底 `else` |
| 循环永不结束 | `while` 条件依赖的变量没有更新 | 在循环体中明确更新，并设置最大轮数 |
| `IndexError` | 直接访问可能为空的列表 | 先判断容器是否为空，或使用 `for` 遍历 |
| `zip` 后少了数据 | 两个序列长度不同，`zip` 按短序列截断 | 需要严格对应时，先用 `len()` 比较长度 |
| `IndentationError` | 同一代码块的缩进不一致 | 统一使用 4 个空格，并让 `elif`/`else` 与 `if` 对齐 |
| 推导式难以阅读 | 嵌套条件或副作用过多 | 拆成普通循环和具名中间变量 |
| 字典遍历拿不到键和值 | 只遍历了键 | 使用 `for key, value in mapping.items()` |

## 本节检查清单

- [ ] 能用 `if/elif/else` 表达互斥的业务分支。
- [ ] 能选择 `for` 或 `while`，并说明循环的退出条件。
- [ ] 能正确使用 `break`、`continue` 和 `enumerate`。
- [ ] 能解释多个 `if` 与 `if/elif/else` 链的执行差异。
- [ ] 能区分推导式中的“过滤条件”和“条件表达式”。
- [ ] 能把简单循环转换为列表或字典推导式。
- [ ] 能运行小实验并解释阈值改变带来的结果变化。

## 自测题

<InteractiveQuiz
  title="03. 控制流与推导式 课后互动自测"
  questions={[
    {
      id: "q1",
      type: "single",
      question: "在 Python 的 if / elif / else 分支结构中，以下哪一项说法是正确的？",
      options: [
        "A. 多个 elif 块会并行同时执行",
        "B. 按从上到下的顺序依次判断，一旦某个条件为 True，其后的 elif / else 就会被全部跳过",
        "C. 无论前面的条件是否命中，全局 else 始终会被执行一次",
        "D. if 条件成立后，程序会自动重新跳回 if 开头重新判断"
      ],
      answer: 1,
      explanation: "if / elif / else 是互斥链式结构。程序按顺序评估，只要匹配到第一个 True 分支并执行完该分支代码后，整条链上后续的 elif 和 else 均会被 Lap/跳过。"
    },
    {
      id: "q2",
      type: "single",
      question: "关于 break 与 continue 关键字，以下哪一项说法是正确的？",
      options: [
        "A. break 仅中止并跳出当前最内层循环，continue 跳过当前循环剩余代码并直接进入下一次迭代",
        "B. break 会直接终止整个 Python 程序的运行",
        "C. continue 可以同时跳出两层嵌套循环",
        "D. break 和 continue 只能在 while 循环中使用，不能在 for 循环中使用"
      ],
      answer: 0,
      explanation: "break 只作用于包含它的最内层循环，终止该循环；continue 用于立即放弃当前这轮循环剩下的代码，跳入同层循环的下一次迭代。"
    },
    {
      id: "q3",
      type: "code-output",
      question: "查看以下推导式对比代码。运行后 print(relu_like) 打印的最终结果是什么？",
      codeSnippet: "values = [-2, 0, 3]\npositive_only = [x for x in values if x > 0]\nrelu_like = [x if x > 0 else 0 for x in values]\nprint(relu_like)",
      options: [
        "A. [3]",
        "B. [-2, 0, 3]",
        "C. [0, 0, 3]",
        "D. [0, 3]"
      ],
      answer: 2,
      explanation: "for 后的 if x > 0 是【元素过滤条件】，positive_only 结果为 [3]；而 for 前的 x if x > 0 else 0 是【三元表达式】，对每个元素都保留并转换，小于等于 0 的转为 0，故 relu_like 为 [0, 0, 3]。"
    },
    {
      id: "q4",
      type: "multiple",
      question: "在编写 Python 代码时，在什么情况下应该放弃列表推导式，改回使用普通的 for 循环？（多选）",
      options: [
        "A. 推导式中包含多层嵌套 for 循环或多个复杂分支，导致代码过于冗长难读",
        "B. 循环体内部需要执行打印日志、文件写操作或网络请求等副作用 (Side Effects)",
        "C. 循环内部需要加入复杂的异常捕获 (try / except) 逻辑",
        "D. 当处理的样本数量超过 10 个时"
      ],
      answer: [0, 1, 2],
      explanation: "推导式追求简洁与声明式可读性。当逻辑复杂、包含副作用、或需要异常处理时，普通 for 循环更易阅读与调试。选项 D 不是放弃推导式的依据。"
    }
  ]}
/>


## 下一步

下一章：[04.核心数据结构](/learn/foundations/python/04-py-data-structures)。你将把本节的遍历和分支应用到列表、字典、集合与嵌套数据上。

## 参考资料

- [Python 官方教程：控制流工具](https://docs.python.org/3/tutorial/controlflow.html)
- [Python 语言参考：复合语句](https://docs.python.org/3/reference/compound_stmts.html)
- [Python 官方教程：数据结构](https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions)
